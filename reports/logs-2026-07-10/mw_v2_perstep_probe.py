# per-step NLL profile probe (see session ledger 2026-07-10); result: humped profile —
# steps 0-4: 2.50, 16-48: 3.22 (frontier/occupancy-blind mid-hump), 60-64: 0.68 (sigma 0.33).
# Full probe body lives in the session transcript; rerun by copying the log_prob body with
# per-step lq kept (head.log_prob returns [B,N] pre-sum).
